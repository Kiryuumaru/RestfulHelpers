namespace RestfulHelpers.Common.Internals;

internal class Message
{
    internal const string RequiresDynamicCode = $"Serializable objects must preserve all its required types when trimming is enabled.";
    internal const string RequiresUnreferencedCodeMessage = $"Serializable objects must preserve all its required types when trimming is enabled";
}
